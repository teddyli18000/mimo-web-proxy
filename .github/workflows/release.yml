name: Release

# 打 tag（v*）自动构建三平台产物并发布 pre-release。
# 手动触发（Actions → Release → Run workflow）只构建并上传 artifact，不发布。
on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build ${{ matrix.asset }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            goos: windows
            goarch: amd64
            asset: windows-amd64
          - os: macos-latest
            goos: darwin
            goarch: arm64
            asset: macos-arm64
          - os: macos-latest
            goos: darwin
            goarch: amd64
            asset: macos-amd64
          - os: ubuntu-latest
            goos: linux
            goarch: amd64
            asset: linux-amd64

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: web/package-lock.json

      # static/ 未入库（.gitignore 排除），必须先在 CI 里构建前端，go:embed 才有内容
      - name: Build frontend
        working-directory: web
        run: |
          npm ci
          npm run build

      - uses: actions/setup-go@v5
        with:
          go-version: '1.22'
          cache: true

      - name: Build binary
        shell: bash
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: '0'
        run: |
          mkdir -p dist
          BIN=dist/mimo-web-proxy
          if [ "$GOOS" = "windows" ]; then BIN=dist/mimo-web-proxy.exe; fi
          go build -trimpath -ldflags "-s -w" -o "$BIN" .
          ls -lh dist

      # ---------- Windows：zip ----------
      - name: Package (Windows)
        if: matrix.goos == 'windows'
        shell: pwsh
        run: |
          Copy-Item config.example.json dist/
          Compress-Archive -Path dist/* -DestinationPath "mimo-web-proxy-${{ matrix.asset }}.zip"

      # ---------- Linux：tar.gz ----------
      - name: Package (Linux)
        if: matrix.goos == 'linux'
        shell: bash
        run: |
          cp config.example.json dist/
          tar -czf "mimo-web-proxy-${{ matrix.asset }}.tar.gz" -C dist .

      # ---------- macOS：.app bundle + dmg ----------
      - name: Package (macOS)
        if: matrix.goos == 'darwin'
        shell: bash
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          case "$GITHUB_REF" in
            refs/tags/*) ;;
            *) VERSION="0.0.0" ;;
          esac
          APP="dist/MiMo Web Proxy.app"
          mkdir -p "$APP/Contents/MacOS" "$APP/Contents/Resources"
          mv dist/mimo-web-proxy "$APP/Contents/MacOS/mimo-web-proxy"
          chmod +x "$APP/Contents/MacOS/mimo-web-proxy"

          cat > "$APP/Contents/Info.plist" <<PLIST
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
            <key>CFBundleName</key><string>MiMo Web Proxy</string>
            <key>CFBundleDisplayName</key><string>MiMo Web Proxy</string>
            <key>CFBundleIdentifier</key><string>com.teddyli18000.mimo-web-proxy</string>
            <key>CFBundleExecutable</key><string>mimo-web-proxy</string>
            <key>CFBundlePackageType</key><string>APPL</string>
            <key>CFBundleVersion</key><string>${VERSION}</string>
            <key>CFBundleShortVersionString</key><string>${VERSION}</string>
            <key>LSMinimumSystemVersion</key><string>10.15</string>
            <key>NSHighResolutionCapable</key><true/>
          </dict>
          </plist>
          PLIST

          # 无开发者证书，做 ad-hoc 签名让 bundle 结构合法（不解决 Gatekeeper 拦截）
          codesign --force --deep --sign - "$APP" 2>/dev/null || true

          cp config.example.json dist/
          cat > dist/使用说明.txt <<'TXT'
          MiMo Web Proxy for macOS

          首次打开（未签名，macOS 会拦截）：
            右键点击 "MiMo Web Proxy.app" → 打开 → 在弹窗里再点"打开"
          或执行：
            xattr -dr com.apple.quarantine "/Applications/MiMo Web Proxy.app"

          使用：
            双击 App，浏览器自动打开面板 http://localhost:8080
            在面板里填入 MiMo Cookie 即可

          配置位置：
            ~/Library/Application Support/mimo-web-proxy/
          TXT

          hdiutil create -volname "MiMo Web Proxy" -srcfolder dist -ov -format UDZO \
            "mimo-web-proxy-${{ matrix.asset }}.dmg"
          ls -lh *.dmg

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.asset }}
          path: |
            *.zip
            *.tar.gz
            *.dmg
          if-no-files-found: error

  release:
    name: Publish pre-release
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: List assets
        run: ls -lh artifacts

      - uses: softprops/action-gh-release@v2
        with:
          prerelease: true
          files: artifacts/*
          fail_on_unmatched_files: true
          generate_release_notes: true
          body: |
            > ⚠️ 预发布版本（pre-release）：功能可用但可能仍有问题，欢迎反馈。

            ## 下载

            | 平台 | 文件 | 说明 |
            |---|---|---|
            | Windows 10/11 (x64) | `mimo-web-proxy-windows-amd64.zip` | 解压后双击 `mimo-web-proxy.exe` |
            | macOS (Apple Silicon) | `mimo-web-proxy-macos-arm64.dmg` | M1/M2/M3/M4 |
            | macOS (Intel) | `mimo-web-proxy-macos-amd64.dmg` | x86_64 |
            | Linux (x64) | `mimo-web-proxy-linux-amd64.tar.gz` | 解压后 `./mimo-web-proxy` |

            ## 首次运行

            程序是自包含的：首次启动会在配置目录自动生成 `config.json` 与 `data/`，浏览器自动打开面板 `http://localhost:8080`。

            - **Windows**：配置在 exe 同目录
            - **macOS**：配置在 `~/Library/Application Support/mimo-web-proxy/`
            - **Linux**：配置在二进制同目录

            ### macOS 未签名提示

            本版本未做代码签名/公证，首次打开会被 Gatekeeper 拦截。**右键点击 App → 打开 → 再点"打开"**，或执行：

            ```bash
            xattr -dr com.apple.quarantine "/Applications/MiMo Web Proxy.app"
            ```

            ### Linux

            ```bash
            tar -xzf mimo-web-proxy-linux-amd64.tar.gz
            chmod +x mimo-web-proxy && ./mimo-web-proxy
            ```

            ## 配置客户端

            | 客户端 | Base URL | API Key |
            |---|---|---|
            | OpenAI 兼容（Cherry Studio / ChatBox / Cline 等） | `http://localhost:8080/v1` | `sk-mimo` |
            | Anthropic 兼容（Claude Code 等） | `http://localhost:8080` | `sk-mimo` |

            完整说明见 [README](https://github.com/teddyli18000/mimo-web-proxy#readme)。
